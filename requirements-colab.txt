# Requirements for Google Colab
# Core ML
torch>=2.0.0
numpy>=1.24.0

# Data processing
pandas>=2.0.0
pyarrow>=12.0.0  # For Parquet support
TA-Lib>=0.4.0  # Run: apt-get install -y ta-lib OR see install-talib.sh first

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Visualization (optional but useful in Colab)
matplotlib>=3.7.0
seaborn>=0.12.0
tensorboard>=2.13.0

# Note: websockets and deriv-api not needed for training
# They're only needed for live trading

# Progress bars
tqdm>=4.66.0
